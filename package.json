{
  "name": "notecat",
  "private": true,
  "version": "0.2.6",
  "description": "A minimal notes app with multi-note support",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "lint": "oxlint"
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "vite": "^7.2.4"
  },
  "dependencies": {
    "oxlint": "^1.43.0"
  }
}
